# 贡献指南

欢迎补充格式支持、修复解析问题和改进中文文档。修改时应保留源数据、对象身份和原生引用关系。

## 提交问题

问题说明使用中文，包含编译器与平台、库版本、复现命令、预期结果和实际结果。几何问题尽量说明具体构件或源记录位置；属性问题说明类 ID、对象 ID 和字段路径；结构树问题说明节点身份和关系。

可附带有权分享的最小复现文件或合成输入。完整模型、属性导出和原始日志可能含项目数据，应先缩减到复现所需内容。

## 增加或修正解码

1. 记录结构证据：源流、偏移、字段类型、长度约束和引用作用域。
2. 保留原始载荷。不能确定业务名称时使用明确的未知状态，不以猜测值填充。
3. 几何复用只依据原生身份和引用，不因形状、名字或材质参数相同而合并独立对象。
4. 为修改增加能够复现问题的测试。
5. 运行相关测试，更新受影响的中文文档。

涉及几何变换时检查镜像绕序和 UV 角点；涉及属性绑定或层级时检查模型作用域、重复身份、缺失候选及循环。无法解释的几何应返回明确的未知状态。

## 构建与测试

构建方式见[接入指南](docs/接入指南.md)。测试代码使用合成输入，可以在启用 `P3D_BUILD_TESTS` 后运行：

```sh
cmake --build build --config Release
ctest --test-dir build -C Release --output-on-failure
```

问题或提交说明应包含编译器、平台及复现方法。

## 文档与提交约定

README、设计解释、提交说明和 PR 正文优先使用中文。代码标识符、JSON 键名及第三方许可证保持原文。提交标题直接说明结果，例如：

```text
修复：保留嵌套镜像变换后的 UV 角点对应关系
文档：补充原生实例和材质候选的接入示例
新增：支持额外的材质参数类型
```

正文说明触发条件、修改后的行为、验证方式和仍然存在的限制。文档中的“已支持”“已验证”“未知”应与实际证据一致。

## 第三方代码

更新依赖时保留其许可证和来源，更新 `third_party/manifest.json` 中的校验值，并说明对构建及解析结果的影响。不要把原建模软件二进制或专有运行库加入工程。
